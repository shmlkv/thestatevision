// "use server";
import { NavLink } from "./Navbar";

export default async function DashboardLink({ user }: { user: any }) {
  return (
    <div className="flex flex-row items-center self-center justify-center flex-shrink-0 shadow-md lg:justify-end">

    </div>
  );
}
